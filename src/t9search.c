#include <stdio.h>
#include <stdbool.h>

// 2. According to the number passed in as first argument find the first and
// last index of that number, continue digit by digit until none are left, 
// return all entries that pass the filtering above
// Zvazit porovnavani binarni maskou
// 3. Handle all the inevitable errors 
// 4. Profit

// Checks whether the input str[] contains filter[]
bool filter(char str[], char filter[]){

    return false;
}

int main(){
    return 0;
}


